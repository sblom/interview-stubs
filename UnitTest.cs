using Xunit;
using static Program;

public class UnitTest
{
    [Theory]
    [InlineData(/* INPUT(S), RESULT */)]
    // Repeat InlineData as many times as you'd like
    public void ImplementationReturnsExpectedResults(/* INPUT(S), RESULT */)
    {
        // Assert.Equal(/*RESULT*/, InterviewMethod(/* INPUTS */));
    }
}